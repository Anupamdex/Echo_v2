from tkinter import *
from PIL import ImageTk,Image


def resizer(img, w_value, h_value):
        
    raw = Image.open(img)
    #OPEN and RESIZE image
    resized = raw.resize(((int(h_value)), (int(w_value))), Image.Resampling.LANCZOS)
    final = ImageTk.PhotoImage(resized)
    return final

def main():
    root = Tk()
    root.geometry("800x500")

    one = "files/background.png"
    final_image = resizer(one, 500, 800)

    Label(root, image= final_image, height=500, width=800).place(x=0, y=0)
    root.mainloop()

main()



